#include<stdio.h>

int main (void){
	int month, year;
	printf("Please enter a month, year format in mm-yyyy. \n");
	scanf("%d-%d",month,year);
	if (month > 12 || month <1){
		printf("Invalid month value")
	}
	
	if(year)
}

